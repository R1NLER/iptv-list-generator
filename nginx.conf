events {}

http {
    server {
        listen 80;

        location / {
            root /htdocs;
            autoindex on;
            autoindex_exact_size off;
            autoindex_localtime on;
        }
    }
}